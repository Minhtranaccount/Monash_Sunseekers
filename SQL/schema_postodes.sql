create table main."Postcodes APVI"
(
    postcode            INTEGER,
    instals             INTEGER,
    estimated_dwellings INTEGER,
    density             REAL,
    capacity            INTEGER,
    capunder10          INTEGER,
    cap10_100           INTEGER,
    capover100          INTEGER,
    countunder10        INTEGER,
    count10_100         INTEGER,
    countover100        INTEGER,
    pot_kw              INTEGER,
    pot_sqm             TEXT,
    pot_gwh             REAL
);


